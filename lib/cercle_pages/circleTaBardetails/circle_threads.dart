import 'package:flutter/material.dart';

class CircleThreads extends StatelessWidget {
  const CircleThreads({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Cercle Threads');
  }
}