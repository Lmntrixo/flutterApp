import 'package:flutter/material.dart';

class CirclePost extends StatelessWidget {
  const CirclePost({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('circles posts');
  }
}